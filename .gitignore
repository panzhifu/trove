/target

# 技能安装产物：由 `npx skills add` / WorkBuddy 安装，内容是第三方 Markdown，不该进仓库。
# 只忽略 skills 子目录，保留 .claude/ 下可能存在的自有配置。
.claude/skills/
.agents/
.workbuddy/skills/

# 无关项目的本地打包文件，不属于 trove。
tty7-bin/

# 代码助手留下的计划/草稿目录，工具产物，不进仓库。
.zcode/

# PLY 基准测试的生成数据（最大单文件 573MB，超过 GitHub 限制），由
# crates/trove-core/examples/ply_bench.rs 生成，不进仓库。
bench-data/

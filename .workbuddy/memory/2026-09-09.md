
## 2026-09-09

## 预览弹窗抽成独立组件
- 新 `dialogs/preview.rs`：`PreviewData::load`（名称/类型/缩略图/动图源解析）+ `element()`（动图→静态图→图标回退）+ `open_asset_preview(controller, id, window, cx)`。
- workspace.rs `open_preview` 变三行委托；行为不变。后续 inspector/搜索结果/重复文件可复用。
- 注意 gpui-kit 导入习惯：v_flex 在 gpui_kit::base，open_dialog 需 WindowExt as _；IconName 类型在仅作表达式传递时无需导入。

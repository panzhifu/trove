# 2026-09-08

## 树状标签 + 重复检测方案设计（未实现，待用户点头）
- 树状标签：抄 collections 表的成熟模式——v5 迁移 `ALTER TABLE tags ADD COLUMN parent_id TEXT REFERENCES tags(id) ON DELETE CASCADE` + 父列索引；保持 name 全局唯一（ensure_tag/FTS 不用动）；后代查询用递归 CTE（不建 closure table，规模不需要）；move_to 环检测抄 collections::move_to；undo 模块需补"标签改父级"操作；tags_panel 改树渲染（自绘折叠，复用 inspector collapsible 经验）+ 拖拽改父级。
- 重复检测三层漏斗：① sha256 GROUP BY（idx_assets_sha256 已有，零成本）；② dHash 64-bit 新列（缩略图重建管线顺手算，汉明距离≤8，SQL 粗筛 + Rust 内存精确比较）；③ CLIP embedding 余弦≥0.95（复用 all_embeddings，定位为"相似"非"重复"）。
- 落地顺序建议：重复检测第①层（系统视图"重复项"）→ 树状标签 → dHash 回填 → 人脸识别。
- **用户决策（00:36）：两个都不做。** 理由：已有子集合 + 平铺标签可打多个，够用；认可平铺的简洁。方案存档于本文件，触发条件：标签数量膨胀到侧栏难浏览，或需要"父标签一键含子级"筛选时再议。

## Release 停在 v0.1 的原因与处理
- release.yml 是 **tag 触发**（`on: push: tags: v*.*.*`），Cargo.toml 版本号改动不会自动发版；昨天只打过 v0.1.0 tag。
- 已在 c4a6953（三平台 CI 全绿）上打并推送 `v0.2.1` annotated tag，Release workflow 已触发（4 目标：linux/windows/mac x64/mac arm64，softprops/action-gh-release 自动建 Release + auto notes）。
- 流程：改 Cargo.toml 版本 → commit → 在绿提交上 `git tag -a vX.Y.Z && git push origin vX.Y.Z`。

## README 截图
- 用户提供的应用主窗口截图（剪贴板）入库为 `docs/screenshots/main-window.png`，插入两份 README 的「界面布局 / UI layout」段首。提交 `8f9edee` 已推送。

## 盘点发现（08:53）：另一会话留下 21 个未推送提交
- 昨天 8f9edee 之后，另一会话完成了大批 P0/P1 功能（+5514/-397，62 文件），但**一直没 push**。
- 已验证本地质量：99+2 测试全绿、fmt 干净、clippy 警告 0（顺手修了 library.rs 两个警告，`764acde`），全部 22 个提交已推送，CI 跑起。
- 重要变化备忘：schema v4→**v6**（v5 资产色标、v6 层级标签——之前用户说"先不做"的两个都被另一会话做了）；源码重组 trove-app src/{app,dialogs,library,panels}、trove-core src/services/；app.rs 拆进 app/；FRONTEND-GAPS.md 被 docs/FEATURE-GAPS.md 取代（对标 Eagle/Billfish/digiKam 等 11 款的差距路线图，P0 批次已实现）。
- 新功能清单：重复图查找器（感知哈希聚类）、本地采集服务 127.0.0.1 HTTP inbox + Trove Collector 浏览器扩展（MV3，仓库根 extension/）、粘贴导入/监视文件夹、可移植媒体包（导出/导入）、RAW/HEIC/SVG/PSD 缩略图、批量重命名、元数据回填导入、库备份/统计/最近库、弹窗关闭 X 修复。
- 版本号仍 0.2.1，GitHub Release 最新仍是 v0.2.1（不含这批）；下次发版需 bump 0.3.0 + tag。

## v0.3.0 发布（09:45 完成）
- Windows CI 卡点定位过程：无 token 拿不到日志 → 给 ci.yml Test 步骤加"失败行转 ::error:: 注解"（`Emit test failures as annotations`，bash shell + grep panicked/FAILED/failures:），注解无需权限可读——用户贴来的正是 `backup_creates_prunes_and_throttles` 在 backup.rs:139 崩。
- 根因：测试删除临时目录时 SQLite 连接（root/library.db）还开着，Windows 禁删占用文件（Os error 32）。修复 = 删前 `drop(conn)`（fe1646c）。watcher.rs 等其他 remove_dir_all 检查过无此模式。
- 版本 0.3.0（两 manifest + app/root.rs）。fe1646c 三平台绿 → tag v0.3.0 → Release workflow success，4 assets（linux/win/mac x64/mac arm64）。https://github.com/panzhifu/trove/releases
- 本机 Chrome/agent-browser 因 root 沙箱崩（exit 133），不可用；gpui-pre 不支持跨 target 交叉 check。

## 文档 vs 现状差距盘点（12:52，未改代码）
- 抽查 P0/P1 声明全部属实：color_label v5、tags parent_id v6（ON DELETE SET NULL 晋升）、services/{backup,collect,maintenance}、扩展端口 23916=DEFAULT_PORT、watcher 启动即挂（root.rs:95）、reveal/批量重命名/粘贴导入/统计/最近库均在。
- 文档失真点：① "亮色主题仅暗色"过时——UI 实际跟随系统亮暗（用户截图即浅色），缺的只是手动开关；② 3.7#1 说"媒体本体打包导出仍缺 P2"，但第三批已实现媒体包导出，内部矛盾；③ 设置里无主题切换项。
- 真实剩余：P1 末完成项 = OCR、应用内 URL 导入；P2 大头 = AVIF/JPEG-XL、视频/动图预览、时间线/对比视图、XMP 写回、人脸/打标/中文 CLIP、批量转换；P3 全部未动。rawler LGPL-2.1 合规评估是隐性债务。

## 智能集合新字段（13:24，001e19e 已推送）
- SmartField 新增 CapturedAt / AspectRatio / Orientation，**零迁移**（captured_at/width/height 列本就有）。
- SQL：日期用 `substr(captured_at,1,10)` 与 YYYY-MM-DD 字符串比较（RFC3339 前缀=日，字典序安全）；宽高比 `(width*1.0/NULLIF(height,0))`（无尺寸 NULL 永不匹配）；方向 width vs height 三态，ne 用 NOT 包裹。
- UI：rules.rs 日期/比例走文本输入（valid_date 校验，f64 正数校验），方向下拉（landscape/portrait/square）；allowed_ops 日期/比例六种、方向 eq/ne。
- **顺手修两个上批 locale 缺失**：rules.f_label / rules.no_label 两语言都没有（界面显示原始键名），zh 还缺 inspector.location。现 en/zh 键序重新对齐。
- 测试 +1（smart_collection_date_aspect_orientation），总数 100+2。clippy 剩 15 个警告全为昨晚批次的既有代码（thumb.rs cast、undo.rs 变体大小、items-after-test-module、root.rs let-unit 等），CI 未开 -D 不阻塞。

## 完整性校验（SHA-256 损坏检测）- ca87fd5
- core: services::maintenance 新增 IntegrityPlan/Report，plan/run 分离（plan 主线程、hash 后台 executor）；HashMap 按 blob 路径缓存，共享 blob 只哈希一次；IntegrityIssue = MissingBlob | HashMismatch；verify_integrity 同步便捷入口；blob::hex 改 pub(crate)
- 测试 integrity_check_detects_missing_and_corrupted_blobs（健康→损坏→缺失三阶段）
- UI: 设置▸维护新条目「完整性校验（SHA-256）」；报告存 controller.integrity_report（swap_library 时清空）；问题行带「移到回收站」操作（成功后从报告剔除）
- locale: settings.verify_integrity/verify_start/verify_done_clean/verify_done_issues/verify_missing/verify_corrupted（en/zh 同序）

## 预览对话框缩放滑块移除 - b8bfdc5
- open_preview 删除自制 zoom slider（轨道/拖块/百分比）+ ZoomSliderState/DragData 结构体；图片保持 max 720x520 Contain 适配

## CI 排查结论
- 29d0d2b 三平台 failure 是假警报：随后推送 ca87fd5 触发 ci.yml cancel-in-progress，前一 run 被掐断记为 failure；ca87fd5 ubuntu/macos 均 success 证实无回归
- 本地踩坑：cargo test 链接 trove-app 测试二进制偶发 glibc "failed to register TLS destructor" SIGABRT，系统内存压力大时出现；-j 2 降低并行可稳定复现成功

## URL 导入（粘贴链接抓取文件）- 994014f
- core: collect::fetch_to_inbox(url) —— fetch(ureq, 512MB cap / 60s 全局超时) + suggested_name（URL 路径取名）+ save 进 inbox 并写 source_url sidecar；仅接受 http/https
- UI: 文件菜单「从 URL 导入…」→ prompt 对话框（InputState + dialog.on_ok 返回 bool=true 关闭）→ 后台下载 → jobs::collect_inbox_app 排空 inbox（复用现有导入管线，source_url 写入 assets，完成后清理 sidecar）
- 正在导入时 collect_inbox_app 返回 false → 文件留在 inbox，watcher 下轮自动导入（notice.import_url_queued 提示）；下载失败 notice.download_failed
- gpui-kit 对话框要点：open_dialog 闭包必须返回 Dialog 值（builder 链结尾不能有分号）；on_ok 签名 Fn(&ClickEvent,&mut Window,&mut App)->bool；AnyWindowHandle 是 Copy 无需 clone

## 链接导入 + 颜色组件 + UI 修复 - 8fbdb7f
- 链接导入：AppConfig.import_mode("copy"默认/"link") + import_linked()；stage_all/stage_source 加 linked 参数——linked 时 blob::hash_file 就地哈希（不复制）、缩略图仍从原文件生成、commit 落 origin=Linked + rel_path=None + extra.source_path 存原路径；jobs::import_paths_app_into 读配置（watcher/拖放/粘贴随之生效），collect inbox 强制 false；去重 heal 条件加 origin==Stored 防止把 linked 记录转成 stored；Inspector location 行对 linked 显示 extra.source_path + 「链接」徽标（inspector.linked），linked 字体预览直接读原文件
- 颜色组件：新模块 panels/color_label.rs（label_name / LabelTarget::{Selection,Primary} / apply / menu_entries / picker）；Inspector color_label_row 换用 picker（左键作用于主选，右键弹功能菜单）；workspace_context_menu 委托 menu_entries；网格工具栏新增按颜色过滤（controller.filter_color → AssetQuery.color_label，store 原生支持；trash/recent 视图忽略）
- 修复：inspector 类型按钮组漏 Font；panel.folders 键 en/zh 缺失导致面板标题显示原始键名
- gpui-kit 坑：context_menu 返回 ContextMenu<Stateful<Div>>（不是 Div），需 ContextMenuExt trait

## 文档标记已实现功能 - 05c8686
- FEATURE-GAPS.md：版本标到 0.3.0；能力总述重写；标记 ✅ 的项：URL 导入、链接原文件（基础版，重连是后续 P1）、资产色标（+按颜色筛选/组件化）、智能集合新字段、最近查看历史、完整性校验；新增「四点七、第五批实现」章节
- README.md / README.zh.md：新增「最近查看与库健康 (0.3)」章节、收集入口补 URL 导入与导入方式、色标段落补工具栏按颜色筛选、UI 布局表加最近查看、数据表加 view_history、测试数 99→104

## 92fd0ab — 色卡右键搜同色 + 删除标题栏颜色筛选
- 用户纠正：标题栏颜色筛选下拉删除（controller.filter_color、ViewKey、AssetQuery 传参、locale filter_color/filter_all_colors 全部移除）。
- Inspector 色卡右键改为上下文菜单：「搜索相同颜色的图片」（inspector.search_same_color）调 `workspace_search::open_color_search` → core `visual_search::search_by_color(conn, hex, 50)`（合成单色直方图 + dominant_colors 加成），结果复用 show_results_dialog，模式标签用新键 workspace.color_search；「复制色值」（inspector.copy_hex）保留原剪贴板行为。
- 实现要点：色卡外包 Stateful div + ContextMenuExt::context_menu（需 .id(format!)）；locale 增 inspector.search_same_color/copy_hex、workspace.color_search，删 filter_color/filter_all_colors，parity OK。
- 质量：fmt/clippy 0、104 core + 2 app tests 全过。

## 7fb0a2f — 链接文件重连 + GIF/WebP/APNG 动图播放
- 重连：core `Library::relink_asset(id, path)`（校验 Linked + is_file + SHA-256 一致后重写 extra.source_path，含测试）；Inspector 位置行 linked 资产显示 danger 色「文件丢失」徽标 + RotateCw 重新链接按钮 → `cx.prompt_for_paths`（App 层方法，AsyncApp::update 返回值不是 Result，`let _ =` 会触发 clippy unit-value 告警）。
- 动图：gpui-pre 0.3.3 的 img() 对文件路径（Resource::Path → fs::read → guess_format）自动解码 GIF/动态 WebP 全帧并播放（frame_index 轮换 + request_animation_frame，reduce_motion 时停）；APNG 不支持 → common.rs `decode_apng`（image::PngDecoder::is_apng/apng/into_frames，需 use image::{ImageDecoder, AnimationDecoder}；RGBA→BGRA 用 as_chunks_mut::<4>）；`animated_preview_source(mime, original)` 缓存（OnceLock<Mutex<HashMap>>，负缓存、上限16）；接入 open_preview（双击弹窗）与 Inspector 预览；网格/列表保持缩略图。
- 依赖：workspace 加 image 0.25(png) + smallvec；新 locale 键 inspector.missing/relink/relink_prompt、notice.relink_done/relink_failed。
- 文档：FEATURE-GAPS #7 重连 ✅、3.2#4 动图 ✅（第六批实现）；README 中英同步（测试数 105）。
- 质量：fmt/clippy 0、105 core + 2 app tests 全过。

## 0c77c13 — 字体样张卡片 + 字体视图 + 系统安装
- 用户先要方案后确认：网格预览选 A1（缩略图卡片）、集合选 B1（系统视图）、安装选 Linux 用户级。
- 字体卡片：thumb.rs `write_font_card`（fontdue 光栅化 512×256，样张取 AppConfig::font_sample_text() 默认 "Aa 允 123"，缺字跳过、空格词距保留，RGBA 混合写 JPEG）；ensure/regenerate 加 Font 分支；maintenance ThumbPlan.items 带 kind，plan 覆盖 Image+Font（linked 无 rel_path 不在重建范围，导入时生成）。
- 字体视图：复用 kind 过滤（controller.select_fonts 设 filter_kind=Font），explorer 伪行 fonts_count + 高亮条件 filter_kind==Font，title_label 加分支 —— 不需要新 workspace 查询分支。
- 系统安装：trove-app/src/fonts.rs（install/uninstall/is_installed，Linux ~/.local/share/fonts/trove + fc-cache -f，macOS ~/Library/Fonts），Inspector font_section 加 sha 参数 + 安装/卸载按钮 + 已安装徽标（按钮状态下次渲染刷新，用 cx.refresh_windows()）。
- 设置：SettingField::input(value_fn, set_fn) 绑定 font_sample；dep fontdue 0.9（core）、dirs（app）。
- 测试：font_card_generated_from_system_font（无系统字体则 skip）。106 core + 2 app 全过。

## 修复：字体视图同时高亮「全部资产」
- explorer.rs all_selected/fonts_selected 计算顺序问题：fonts 视图复用 all-assets 查询（filter_kind=Font），all_selected 未排除该状态。改为先算 fonts_selected，all_selected 排除之。

## 网格字体实况渲染
- Cell 增加 font_family/font_blob（Font 时从 extra.font_family + rel_path/source_path 取）；build_cell_element/build_list_row_element 对注册成功的字体用 common::font_live_preview 实时渲染单行样张（text_size 随格高缩放，列表 60×36 @18px），失败回退样张卡片图。
- common.rs：全局 REGISTERED_FONTS（OnceLock<Mutex<HashSet>>）ensure_font_registered；cached_font_sample 2s TTL 缓存（避免每格渲染读配置文件）；inspector 的 font_previews 字段删除、注册逻辑统一委托 common。

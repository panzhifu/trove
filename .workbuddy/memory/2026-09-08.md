# 2026-09-08

## 树状标签 + 重复检测方案设计（未实现，待用户点头）
- 树状标签：抄 collections 表的成熟模式——v5 迁移 `ALTER TABLE tags ADD COLUMN parent_id TEXT REFERENCES tags(id) ON DELETE CASCADE` + 父列索引；保持 name 全局唯一（ensure_tag/FTS 不用动）；后代查询用递归 CTE（不建 closure table，规模不需要）；move_to 环检测抄 collections::move_to；undo 模块需补"标签改父级"操作；tags_panel 改树渲染（自绘折叠，复用 inspector collapsible 经验）+ 拖拽改父级。
- 重复检测三层漏斗：① sha256 GROUP BY（idx_assets_sha256 已有，零成本）；② dHash 64-bit 新列（缩略图重建管线顺手算，汉明距离≤8，SQL 粗筛 + Rust 内存精确比较）；③ CLIP embedding 余弦≥0.95（复用 all_embeddings，定位为"相似"非"重复"）。
- 落地顺序建议：重复检测第①层（系统视图"重复项"）→ 树状标签 → dHash 回填 → 人脸识别。
- **用户决策（00:36）：两个都不做。** 理由：已有子集合 + 平铺标签可打多个，够用；认可平铺的简洁。方案存档于本文件，触发条件：标签数量膨胀到侧栏难浏览，或需要"父标签一键含子级"筛选时再议。

## Release 停在 v0.1 的原因与处理
- release.yml 是 **tag 触发**（`on: push: tags: v*.*.*`），Cargo.toml 版本号改动不会自动发版；昨天只打过 v0.1.0 tag。
- 已在 c4a6953（三平台 CI 全绿）上打并推送 `v0.2.1` annotated tag，Release workflow 已触发（4 目标：linux/windows/mac x64/mac arm64，softprops/action-gh-release 自动建 Release + auto notes）。
- 流程：改 Cargo.toml 版本 → commit → 在绿提交上 `git tag -a vX.Y.Z && git push origin vX.Y.Z`。

## README 截图
- 用户提供的应用主窗口截图（剪贴板）入库为 `docs/screenshots/main-window.png`，插入两份 README 的「界面布局 / UI layout」段首。提交 `8f9edee` 已推送。

## 盘点发现（08:53）：另一会话留下 21 个未推送提交
- 昨天 8f9edee 之后，另一会话完成了大批 P0/P1 功能（+5514/-397，62 文件），但**一直没 push**。
- 已验证本地质量：99+2 测试全绿、fmt 干净、clippy 警告 0（顺手修了 library.rs 两个警告，`764acde`），全部 22 个提交已推送，CI 跑起。
- 重要变化备忘：schema v4→**v6**（v5 资产色标、v6 层级标签——之前用户说"先不做"的两个都被另一会话做了）；源码重组 trove-app src/{app,dialogs,library,panels}、trove-core src/services/；app.rs 拆进 app/；FRONTEND-GAPS.md 被 docs/FEATURE-GAPS.md 取代（对标 Eagle/Billfish/digiKam 等 11 款的差距路线图，P0 批次已实现）。
- 新功能清单：重复图查找器（感知哈希聚类）、本地采集服务 127.0.0.1 HTTP inbox + Trove Collector 浏览器扩展（MV3，仓库根 extension/）、粘贴导入/监视文件夹、可移植媒体包（导出/导入）、RAW/HEIC/SVG/PSD 缩略图、批量重命名、元数据回填导入、库备份/统计/最近库、弹窗关闭 X 修复。
- 版本号仍 0.2.1，GitHub Release 最新仍是 v0.2.1（不含这批）；下次发版需 bump 0.3.0 + tag。

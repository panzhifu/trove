# 2026-09-07

- 收藏视图 + 类型过滤（清单第 4 项）完成：
  - workspace 标题栏新增常驻过滤控件 `filter_controls`：类型下拉（复用 gpui-kit PopupMenu 模式）+ 收藏心形 toggle（primary 高亮）+ 清除按钮；回收站视图隐藏且不套用过滤。
  - trove-core：`smart::evaluate_filtered(conn, node, kind, favorite, limit, offset)`——注意必须先给编译出的树整体加括号再追加 AND（`(a) OR (b) AND kind=?` 优先级 bug 是本轮踩过的坑）；`Library::evaluate_smart_collection` 签名扩为 (id, kind, favorite, limit, offset)。
  - 标题：无命名视图且 filter_favorite 开启时显示「收藏」（workspace.rs 与 explorer.rs 两处 title_label）。
- 维护工具入 Settings + 库热切换（清单第 5 项）完成：
  - settings.rs 重写：`SettingsDialog::open(window, cx, controller)` 持有 controller；新增「维护」页（缩略图增量/全量重建走 background_executor——Library 非 Send，只能主线程做计划、后台只做文件工作；索引重建/孤儿清理同步）；Browse 改为热切换 `swap_library` + 持久化，失败写 notice。
  - state.rs 新增 `notice: Option<String>`、`busy: bool`（防止维护任务并发）。
  - 异步回调拿不到 window，通知走 controller.notice + refresh_windows，不用 push_notification。
- 修复智能集合 `missing field 'op'`：兼容垫片 `compat_insert_match_tag`（缺 op 有 field → 补 "match"，递归），救回旧版本保存的集合；54 测试全绿。
- 构建注意：本机直接 `cargo build` 链接 OOM（LLVM out of memory），需 `cargo rustc -p trove-app -- -C link-arg=-Wl,--threads=1`；cargo 需 export HOME=/home/noke（终端默认 root 且 HOME 为空）。
- FRONTEND-GAPS.md 状态已更新（4/5 两项标 ✅）。
- 续：规则编辑器报错 i18n（颜色/字节非法值给本地化提示，recompute 的 serde 原始错误改为「规则无效」标签，新增 rules.invalid_color / rules.invalid_bytes）；构建产物验证通过。执行环境无图形会话，GUI 冒烟留待用户桌面侧。已沉淀用户级技能 ~/.workbuddy/skills/trove-build/SKILL.md（构建 OOM 绕行、HOME 导出、非 Send 约束等）。
- 续 2：导入进度接入通知层 + 前后端职责迁移：
  - jobs.rs 瘦身为纯编排：`import_paths_app(controller, paths, window, cx)` 新增 window 参数（开始时 info toast、完成时 success/warning toast，用 `Notification::id1::<ImportNotice>("import-progress")` 键控替换同一 toast）；调用点 app.rs prompt_import 用 `window.window_handle()` 捕获，异步回调 `handle.update(cx, |_view, window, cx| ...)`——gpui 的 AsyncApp::update_window 闭包是 3 参（AnyView, &mut Window, &mut App）。
  - 批量导入业务逻辑迁到 trove-core：`import::stage_all(root, paths) -> Vec<std::result::Result<StagedFile, ImportSkip>>`（注意 import.rs 里 `Result` 是单参 error 别名，双参须写全称）+ `import::commit_staged_all(store, into, auto, staged) -> ImportReport`；`import_files_assigned` 改为二者组合。
  - 缩略图重建计划逻辑迁到 trove-core：`maintenance::plan_thumbnail_rebuild(lib, force) -> ThumbPlan{items, missing_blobs}`（主线程，需 Library）+ `run_thumbnail_plan(root, plan) -> ThumbRebuildReport`（Send，后台线程）；settings.rs rebuild_thumbs 改用之，`rebuild_thumbnails` = plan+run。
  - `TROVE_LIBRARY_DIR` 环境变量覆盖逻辑从 app.rs 迁入 `AppConfig::resolved_library_path`。
  - 新测试 batch_pipeline_collects_skips_and_dedupes；55 测试全绿。
- 续 3：排查 Settings/智能集合弹窗 X 无法关闭（链路：DialogClose → 派发 Cancel → base dialog close_dialog）。写了无头复现测试 dialog_close_test.rs（放 bin 内避免测试目标链接 OOM），需 dev-dep gpui-pre + `#![recursion_limit = "4096"]` + RUST_MIN_STACK=64MB（rustc 宏展开栈溢出），测试尚未跑通即被中断，bug 未修复。
- 续 4：对照 FRONTEND-GAPS.md 盘点进度——通知层/导入进度两行过时已改（✅/🚧），优先级 1–5 全部标 ✅，新增已知 bug 记录（弹窗 X 关闭）。
- 续 5：64MB RUST_MIN_STACK 实测仍不够——rustc 编译 bin 内 gpui 测试照样 SIGSEGV 栈溢出，rustc 提示用 128MB（RUST_MIN_STACK=134217728）。已写入 trove-build 技能。弹窗关闭测试仍未跑通，下次用 128MB 重跑。
- 续 6：用户桌面确认弹窗 X 已可关闭（无需继续排查）；全部调试辅助代码已删除还原（dialog_close_test.rs、main.rs 的 recursion_limit/测试模块挂载、Cargo.toml dev-dep、Cargo.lock），cargo check 干净。trove-build 技能中的「编译含 gpui 测试」一节保留备用。
- 续 7：完成 FRONTEND-GAPS 剩余项并提交。本轮补齐：状态栏（app.rs 底部常驻：选中数/库路径/导入进度/notice，AppView observe controller）、导出库接线（File▸导出素材库 → prompt_for_new_path → export_metadata JSON，主线程序列化）、i18n 键补全（workspace 排序/视图、tags 重命名/颜色、inspector 替换标签、statusbar 新节、app.export_*）。修编译坑：gpui-kit 的 FluentBuilder 在 `gpui_kit::prelude::`（不是 component::prelude）；PopupMenuItem on_click 是 Fn，捕获值须在闭包内 clone 再进内层 FnOnce；ImportPhase 变体是 Running{total,done}；context_menu 回调签名 (menu, window, cx)。59 测试全绿，二进制构建通过。FRONTEND-GAPS.md 已同步（剩余项见文档末尾）。
- 续 8：集合拖拽重排/改父级 + 逐文件导入进度完成并提交。
  - 拖拽：common.rs 新增 CollectionDrag(Uuid)；explorer 集合行 on_drag + drag_over/on_drop（拖到集合=改子级末尾，拖到「全部资产」=回根级），AssetsDrag 与 CollectionDrag 两套 payload 在同一行共存；CollectionDragPreview 用 Folder 图标。
  - 导入进度：jobs.rs 改为逐文件 commit_staged 循环，每文件后 handle.update 推键控 toast（done/total）+ timer(1ms) 让帧，状态栏实时刷新。
  - 59 测试全绿、二进制构建通过；FRONTEND-GAPS.md 更新（剩：标签拖出、撤销/重做等）。
- 续 9：撤销/重做完成并提交。core 新增 undo.rs（Op 枚举 + UndoStack 双栈 + SharedUndoStack(RefCell) 嵌入 Library，&self 门面不变）；可撤销：patch_asset（全列逆补丁）、trash/restore、favorite、set_asset_tags/tag_assets、rename_tag/set_tag_color、rename_collection/move_collection、集合成员增删（记录实际 delta）；不可撤销：purge/清空回收站/导入/删除标签/删除集合/智能集合。前端：Ctrl+Z/Ctrl+Shift+Z 绑 Workspace context（输入框文本撤销不受影响）+ Edit 菜单项 + 状态栏 ↩n ↪m 计数；所有变更调用点改走记录型门面。62 测试全绿。坑：record 只记录不执行（测试须先 apply 再 record）；UndoStack.record 清 redo 不清 undo。
- 续 10：CLIP 图片搜索优化落地（在用户未提交 WIP 基础上修编译 + 重构）：
  - P0：Embedding::cosine 去掉 assert（维度不符返 0.0）；semantic_search 显式跳过维度不符行并计数提示（换模型不再 panic）；修 jobs.rs 两个编译错误（Entity::spawn 不存在→并入 import 异步块；AsyncApp 无 read→update 直返 R）。
  - P1 收口：embedding 全部 SQL 下沉 store/assets.rs（set_embedding/has_embedding/images_missing_embedding/all_embeddings 分批 500 行/embedding_counts）；clip.rs 只留推理+embed_asset/embed_all_missing/semantic_search(min_similarity 可配)；Library 新门面 semantic_text_search/semantic_image_search/embed_missing_all/embed_one/embedding_status；state/workspace/workspace_search 全改走门面。
  - P3：导入后自动嵌入逐帧让步（clip::embed_asset，i18n 通知 notice.embedded_done）；settings embed-all 从整批卡死改为一图一帧循环（走 embed_one）；新增「嵌入覆盖度」设置行（embed_coverage*）；workspace 标题栏加 CLIP 语义搜索 toggle 按钮（接通 toggle_semantic_search）；clip.rs 调试 eprintln 清理。
  - P2：阈值 semantic_min_similarity 进 config（默认 0.2，clamp 0..1）；向量缓存/向量索引留给库规模上来再做。79 测试全绿、构建通过。
- 续 11：应要求移除 CLIP 手动开关——搜索框改为自动融合：FTS 结果在前，追加 semantic_text_search 去重命中（semantic_ready 才跑，失败静默降级），total 相应累加；删除 state.semantic_search/toggle 及 locale 键；state 的薄包装 method 一并删除（workspace 直调 Library 门面 + config 阈值）。
- 续 12：错误处理优化收尾并提交（97c0a46）。workspace 查询/smart/semantic 三处 Err 分支统一走新增 `WorkspacePanel::report_view_error`（内部用 state.report_error 去重，同消息不重复 notify，防渲染循环）；settings.rs E0521 修法：language_page 入口 `let controller = controller.clone();` + dropdown 回调改 move 闭包捕获所有权；i18n::set_language 返回 Result（持久化失败经 settings.language_save_failed 提示，locale 本身始终生效）；clip.rs 维度不符 eprintln 改 AtomicUsize 计数 + Settings 覆盖度行警告（settings.dim_mismatch）；main.rs 删多余 eprintln。locale 新键 query_failed/dim_mismatch/language_save_failed 双语对齐（校验脚本 ALIGNED）。79 测试全绿、零警告、二进制构建通过。坑：上轮 Python 脚本替换 workspace.rs anchor 静默失败（打印 ok 但没写入），locale en.toml heredoc 因 `\'` 转义+em-dash 报 SyntaxError——本轮改用 Edit 工具直接编辑 locale，写完跑键对齐校验。
- 续 13：推送 97c0a46 到 origin（4d3624e..97c0a46）；全仓 grep 确认业务代码零 println（唯一残留在 thumb.rs 测试 skip 路径，合法）；FRONTEND-GAPS「eprintln 清理」条目改 ✅ 并提交推送（134d291）。剩余三项（标签拖出、GUI 快捷键回归、导出/导入媒体还原）经询问用户后决定全部挂起，暂停。
- 续 14：修「以文搜图完全失效」（用户日志：Missing Input: pixel_values）并提交推送（5aac7db）。两个根因：① 合并图要求全部输入，text_embedding 只喂 input_ids/attention_mask → vision 分支 Shape 节点缺 pixel_values 使整次 run 失败（修法：补零值 1×3×224×224 dummy pixel_values，与 image 分支喂 dummy text 对称）；② 原 tokenize() 是 FNV 哈希假分词器，token id 全是垃圾 → 新建 media/tokenizer.rs，忠实移植 OpenAI simple_tokenizer.py（bytes_to_unicode、BPE 逐对合并取最低 rank、SOT 49406/EOT 49407、pad 77）。关键细节：attention mask 必须按 token 数量生成而非 `id != 0`——CLIP 词表里 id 0 是合法 piece（"!"）。词表 bpe_simple_vocab_16e6.txt（~1.3MB）需手动下载放模型目录（与项目零构建期网络哲学一致），缺失只禁用文本搜索、不影响图片嵌入；settings 状态行显示 vocab_missing 提示 + 预期路径。中文查询：分词器字节级编码可编码中文，但模型以英文 caption 训练，中文效果有限（需告知用户）。regex = "1" 进 trove-core 依赖。搜索框「回车后保持 + ✕ 关闭」df557e8 已实现（用户描述即现状）；搜索激活时标题栏的 + 图标 = 保存为智能集合（设计内）。81 测试全绿、构建通过。
- 续 15：词表配置落地 + 修解析坑（94101b2 推送）。词表实际是 gzip（bpe_simple_vocab_16e6.txt.gz，仓库里 .txt 裸链 404），curl|gunzip 直写 ~/.config/trove/models/ 成功（3.1MB，262145 行；注意沙箱会把写到 /tmp 和配置目录的文件吞掉，必须一条命令内完成）。关键坑：发布文件首行是 `"bpe_simple_vocab_16e6.txt#version: 0.2`（文件名嵌在 header 里），OpenAI 加载器无条件跳过首行，我的解析器校验 #version 前缀会直接报错——改为无条件跳过。新增 real_vocab_if_present 测试：默认模型目录有真实词表时验证 "a photo of a tree"/"tree"/"一棵树" 编码形状（无词表时静默跳过）。text_embedding 加单槽查询向量缓存（grid 每次 render 重跑搜索，否则每帧 ONNX 推理；configure 时失效）。workspace 空网格加提示（no_results / no_embeddings_hint / no_assets_hint 三态，画在 grid 下层保持鼠标事件）。82 测试全绿。
- 续 16：搜索框改版完成（ab3e5be 推送）——浮层弹出式 + 输入框内 ✕（用户最终确认的交互：点放大镜弹出；有文字时 ✕ 出现在框内右侧；点 ✕ 清空并关闭弹窗）。实现：删常驻胶囊分支；Popover 受控模式 `.open(flag)` + `.on_open_change`（gpui-component 0.6 支持编程式开关，文档要求二者必须配合）；open 状态存 `Rc<Cell<bool>>`（workspace.rs 里 Cell 已 as CellFlag 导入）——因为 ✕ 的 on_click 运行在 popover 上下文拿不到 WorkspacePanel，用共享 cell + refresh_windows 同步；✕ 实时显隐靠 subscribe_in 里补 `InputEvent::Change → cx.notify()`（gpui 的 content 闭包只在整面板重渲染时重跑，不监听就只有下次交互才出现 ✕）；search_active 时放大镜 primary 高亮。编译零警告、构建通过。
- 续 17：库数据诊断（只读 sqlite3）——真实库在 ~/.trove/library/library.db（~/.config/trove/library.db 和 ~/.trove/library.db 是 0 字节空壳）；15 资产（14 图 + 1 字体），14 张图全部有 512 维向量（ViT-B/32 匹配，无旧维度残留）。数据侧就绪，重启应用后 tree/a photo of a tree 应可命中；若不中可调 config 的 semantic_min_similarity（默认 0.2，当前未写 → 默认值）。还原了用户 WIP 遗留在根 Cargo.toml 的 `tokenizers` 依赖（HF tokenizer.json 方案已被手写 BPE 取代，无 crate 引用；git checkout 即回滚）。
- 续 18：搜索框抽成独立组件（ccaf0eb 推送）。新建 panels/search_box.rs：SearchBox 作为自有 Render Entity，自持 input/open(Rc<Cell<bool>>)/controller，订阅 Change（cx.notify 自身）与 PressEnter（set_search）；workspace.rs 删掉 search_input/search_open 字段与 ~80 行 Popover 构造，title_bar 里 `.child(self.search_box.clone())` 一行接入。附带优化：打字时只重渲染搜索框（Entity 隔离），不再整面板重渲染。gpui 的 `child(Entity<T: Render>)` 直接可用；on_open_change/✕ 回调里用 `cx.entity()` 拿自身 Entity 来 notify。
- 续 19：README 双语补「语义搜索（CLIP，可选）」章节（7e9130f 推送）——融合搜索、以图搜图、自动嵌入/补齐、三个手动下载前置（runtime 库/model.onnx/BPE 词表）、英文查询效果提示；media/ 目录树注释与已接通功能状态行同步。远端至 7e9130f。挂起：GUI 验证（用户侧）、标签拖出、媒体还原。
- 续 20：搜索弹窗常驻（3eebd74 推送）。回车提交后不再关闭：PressEnter 里 open.set(true) + cx.notify()，受控 Popover 每次重渲染 sync_open 会把弹窗钉回打开态；✕ 无条件常驻（有文字=清空+关闭，无文字=仅关闭）。顺手补 cx.observe(controller) 让放大镜高亮实时跟随提交的搜索。gpui-base Popover 机制备忘：use_keyed_state 按元素 id 持久化 PopoverState；受控 .open() 走 sync_open(announce=false) 不触发 on_open_change，trigger 切换走 toggle_open(announce=true) 会触发；Escape/点击外部/dismiss 会关。零警告、构建通过。
- 续 21：store 写操作收口 Library 门面（0ed589c 推送）。审计 30+ 个 library.store().conn() 直连点：仅 5 处是写操作——tags_panel 删标签、explorer 智能集合改名/删除（后两者 Library 已有方法，加了重复定义报 E0592 后删掉）、inspector 两处 tags::ensure_named（新增 Library::ensure_tag / delete_tag）。其余全是只读视图查询（网格分页/标题/计数），保留直连并在 FRONTEND-GAPS 标 🚧 说明分层约定（变更走门面、只读可直连、导入逐文件提交走 import:: 为进度上报）。82 测试全绿、零警告、构建通过。
- 续 22：修 Esc 无法关闭 explorer 行内编辑器（c88155d 推送）。机制：gpui-base Input 绑了 escape→Escape action，默认 clean_on_escape=false 时 handler 会 cx.propagate()——在 Explorer 面板根加 key_context("Explorer") + on_action 接住。实现：actions.rs 新增 CancelEditor；main.rs 固定绑定 escape→CancelEditor(EXPLORER_CONTEXT)；explorer.rs cancel_editor 方法（mode 非 None 才重置 + 清空输入）。对重命名/新增编辑器都生效。零警告、构建通过。
- 续 23：clippy 全量清零（37→0）+ 技能沉淀。cargo clippy --fix 处理 30 个机械 lint；手修 7 个：library.rs 冗余 let tx=tx、rows.rs loop→while let、visual_search.rs 去 to_string、settings.rs range 循环改迭代器、clip.rs 文档缩进×5。82 测试全绿、构建通过。trove-build 技能追加两节：CLIP 语义搜索要点（合并图全输入、词表加载/下载坑、换模型流程、Eagle 对比）与 gpui-kit 弹层/键盘（Popover 受控模式 sync_open 语义、content 闭包签名与 Rc<Cell> 共享、escape propagate 面板拦截、Entity<Render> 子组件模式）。
- 续 24：门面方法补测试（tag_and_smart_collection_facade_methods：ensure_tag 幂等、delete_tag 解绑+删行、智能集合改名/删除）+ README 测试数 75→83。83 测试全绿、clippy 零警告、构建通过。

## 续 25（检查器折叠分块 + 标签面板添加按钮 + MIT License）
- inspector.rs：新增 `collapsed: HashSet<&'static str>` 字段 + `collapsible_section(id, title, cx, content)` 辅助方法；编辑/标签/属性/颜色/字体五块改为可折叠，预览保持置顶。坑：gpui-base 的 Div **没有** `.rotate()`，旋转要放在 `Icon` 上（`Icon::rotate(percentage(...))`，见 gpui-component icon.rs）；chevron 开 0 / 收 -0.25。
- tags_panel.rs：节头右侧 "+" 按钮（IconName::Plus）→ `open_create_dialog`（复用 rename 对话框模式）→ `Library::ensure_tag` + generation+1；locale 新增 tags.add_tag / tags.create_tag。
- License：Apache-2.0 → MIT（新建 LICENSE，两个 crate manifest 加 `license = "MIT"`，两份 README）。版权人 panzhifu。
- clippy 清零 5 个测试警告（map_identity / manual_contains×2 / double_ended_iterator_last→next_back / unused Utc）。
- **环境坑**：/tmp 是 10MB tmpfs，cc 编译 sqlite 报 No space left on device → `mkdir -p ~/.cache/build-tmp && export TMPDIR=...`；跑 cargo test 需 `RUSTFLAGS="-C link-arg=-Wl,--threads=1"`（触发依赖全量重编）。已写入 trove-build skill。
- 83+1 测试全绿，clippy 0，提交 `6c5bba7` 已推送。

## 续 26（收尾：locale 键序对齐 + 文档同步）
- 发现并修复 en/zh locale 键集相同但顺序分叉（上轮 no_results 等插错位置 + query_failed）：现在两文件键名与顺序完全一致；键序校验脚本思路记录：逐行 strip 后跳过 [section] 收集 key 比较。
- FRONTEND-GAPS.md 补两行：标签创建独立入口（ensure_tag + "+" 按钮）、Inspector 折叠分块。
- 提交 `fd3c674` 已推送。本阶段无遗留任务；workspace.rs 拆分方案（grid_geometry/selection/menus 三切）用户仍选择暂不动。

## 续 27（修复启动 panic + 0.2.1）
- 启动即崩 `Percentage must be between 0 and 1`（gpui-pre geometry.rs）：inspector 折叠箭头 `percentage(-0.25)` 非法。gpui 的 percentage() 不接受负值 → 改 0.75（270° 顺时针，视觉同为朝右）。
- 版本 0.2.0 → 0.2.1（两个 manifest + app.rs 关于页硬编码字符串，共三处）。
- 83+1 测试全绿；`cargo rustc -p trove-app -- -C link-arg=-Wl,--threads=1` 构建成功（target/debug/trove-app，约 797MB debug 产物）。提交 `a1f49e6` 已推送；percentage 负值坑已写入 trove-build skill。

## 续 28（CI rustfmt 修复）
- CI（macos-14 Check job）因 `cargo fmt --check` 失败挂掉：本地从未跑过 cargo fmt，tags_panel/workspace_search/settings/state 等 15 文件有格式差异。
- 已 `cargo fmt --all`（fmt-only，cargo check 通过）并提交 `c4a6953` 推送；trove-build skill 常用命令加了"推送前必跑 fmt --check"。
